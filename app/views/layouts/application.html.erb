<!DOCTYPE html>
<%=#Our display.  There's nothing exciting here, just styles and tags with our topbar.
%>
<html>
<head>
  <%= stylesheet_link_tag "https://fonts.googleapis.com/icon?family=Material+Icons" %>
  <meta name="google-site-verification" content="YNqAgQ4ZivsTSMaE2jt6KN38MWuI9gKxjBAkiXUqmkE" /> <!--Import Google Icon Font-->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>BrewHub</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<div id="notificationCard" class="container" style="position: absolute; z-index: 99; top:15%">
  <div class="row">
    <div class="col s4 push-s7">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">I AM COOL</span>
          <p>BECAUSE I CAN MOVE WEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEEE</p>
          </div>
          <div class="card-action">
            <a href="#">ACCEPT</a>
            <a href="#">NAH</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <body style="background-color: RGB(233,235,238)">
    <div class="navbar-fixed">
      <nav class = "top-nav">
        <div class="nav-wrapper brown darken-2 white-text">
          <a href="/" class="brand-logo" style="margin-top:10px; margin-left:20px;"><%=image_tag("/assets/LOGO.png", :alt => "Home Page", :width =>331, :height => 48)%></a>
          <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
          <ul class="right hide-on-med-and-down">
            <li><a href="/op">Styles</a></li>
            <li><a href="/beers">Beers</a></li>
            <li><a href="/pubs">Breweries</a></li>
            <li><a href="#">Social</a></li>
            <% if current_user && !current_user.name.blank? %>
            <li><a href="/users/<%= current_user.id %>"><%=  current_user.name %></a></li>
            <% elsif current_user %>
            <li><a href="/users/<%= current_user.id %>"><%= current_user.username %></a></li>
            <% else %>
            <li><a href="/login">Login</a></li>
            <% end %>
          </ul>
          <ul class="side-nav" id="mobile-demo">
            <li><a href="/beers">Beers</a></li>
            <li><a href="/pubs">Breweries</a></li>
            <li><a href="#">Social</a></li>
            <% if current_user && !current_user.name.blank? %>
            <li><a href="/users/<%= current_user.id %>"><%= current_user.name %></a></li>
            <% elsif current_user %>
            <li><a href="/users/<%= current_user.id %>"><%= current_user.username %></a></li>
            <% else %>
            <li><a href="/login">Login</a></li>
            <% end %>
          </ul>
        </div>
      </nav>
    </div>


    <script>
    $( document ).ready(function(){})
    {
      $(".button-collapse").sideNav();
    }
    </script>

  </body>
  <div id="mainbox" onclick="moveCard()">
    <%= yield %>
  </div>
  </html>
